/** Barrel for the device profile catalog. */
export {
  DEVICE_PROFILES,
  defaultProfileId,
  getDeviceProfile,
  deviceCapabilities,
  FALLBACK_CAPABILITIES,
  type DeviceProfile,
  type DeviceCapabilities,
  type ProfilePortDefaults,
} from './profiles.js';
